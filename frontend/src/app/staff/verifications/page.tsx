'use client';

/**
 * Staff Verifications Page
 * Staff have read-only + approve/reject permissions (controlled by VERIFY_ID permission).
 * This page reuses the same backend endpoints as the admin verifications page.
 */
export { default } from '@/app/admin/verifications/page';
